# Server Configuration
PORT=3001

# Email Configuration
# For Gmail, use your email address and an App Password (not your regular password)
# To generate an App Password: https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Recipient Email (where you want to receive the contact form messages)
# If not set, emails will be sent to EMAIL_USER
RECIPIENT_EMAIL=your-email@gmail.com

# SMTP Configuration (optional - defaults to Gmail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# For other email providers:
# Outlook: smtp-mail.outlook.com, port 587
# Yahoo: smtp.mail.yahoo.com, port 587
# Custom SMTP: configure as needed
